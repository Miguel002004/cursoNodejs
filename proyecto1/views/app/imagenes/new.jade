extends ../../layout.jade
block contenido
  //enctype es necesaro para enviar archivos. ya que express formidable solamente va a parsear archivos con este atributo
  form(action="/app/imagenes", method="POST", enctype="multipart/form-data")
    h1 Subir nueva imagen
    div(class="form-group")
      label(for="title") Titulo
      input(type="text", name="title", placeholder="Titulo de la imagen", id="title", class="form-control")
    div
      label(for="archivo") Imagen
      input(type="file", name="archivo", id="archivo", class="form-control")
    div
      input(type="submit" value="Iniciar sesion" class="btn btn-info")
    
